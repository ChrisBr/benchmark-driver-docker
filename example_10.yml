benchmark:
  1: 'tmp = {}; i = 0; while i < 1; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  2: 'tmp = {}; i = 0; while i < 2; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  3: 'tmp = {}; i = 0; while i < 3; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  4: 'tmp = {}; i = 0; while i < 4; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  5: 'tmp = {}; i = 0; while i < 5; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  6: 'tmp = {}; i = 0; while i < 6; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  7: 'tmp = {}; i = 0; while i < 7; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  8: 'tmp = {}; i = 0; while i < 8; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  9: 'tmp = {}; i = 0; while i < 9; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  10: 'tmp = {}; i = 0; while i < 10; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
