benchmark:
  1_000: 'tmp = {}; i = 0; while i < 1000; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  2_000: 'tmp = {}; i = 0; while i < 2000; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  3_000: 'tmp = {}; i = 0; while i < 3000; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  4_000: 'tmp = {}; i = 0; while i < 4000; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  5_000: 'tmp = {}; i = 0; while i < 5000; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  6_000: 'tmp = {}; i = 0; while i < 6000; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  7_000: 'tmp = {}; i = 0; while i < 7000; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  8_000: 'tmp = {}; i = 0; while i < 8000; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  9_000: 'tmp = {}; i = 0; while i < 9000; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  1_0000: 'tmp = {}; i = 0; while i < 10000; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
