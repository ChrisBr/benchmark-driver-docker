benchmark:
  10: 'tmp = {}; i = 0; while i < 10; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  20: 'tmp = {}; i = 0; while i < 20; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  30: 'tmp = {}; i = 0; while i < 30; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  40: 'tmp = {}; i = 0; while i < 40; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  50: 'tmp = {}; i = 0; while i < 50; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  60: 'tmp = {}; i = 0; while i < 60; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  70: 'tmp = {}; i = 0; while i < 70; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  80: 'tmp = {}; i = 0; while i < 80; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  90: 'tmp = {}; i = 0; while i < 90; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
  100: 'tmp = {}; i = 0; while i < 100; i += 1; tmp[i.to_s] = i; tmp[i.to_s]; end;'
